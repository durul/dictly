# ──────────────────────────────────────────────────────────────────
# Private / internal — never publish to GitHub
# ──────────────────────────────────────────────────────────────────

# Internal engineering notes
CLAUDE.md
ARCHITECTURE.md

# Design source-of-truth (icons, SVGs, design tokens spec, handoff doc)
handoff/

# Local-only signing config: keeps the personal Apple Team ID out of the public
# repo. Base.xcconfig (committed) optionally includes it as the project's base
# configuration, so Xcode and xcodebuild sign with the team automatically.
Local.xcconfig

# Local Claude/agent state
.claude/
.cursorrules

# ──────────────────────────────────────────────────────────────────
# Bundled Whisper models. The `base` variant (~139 MB) ships in-repo so
# the app works offline straight after clone. Larger variants (large-v3
# at ~547 MB etc.) are too big for plain git and must be fetched on
# demand with `python3 scripts/fetch_bundled_model.py <variant>`.
# ──────────────────────────────────────────────────────────────────
Dictly/BundledModels/*
!Dictly/BundledModels/openai_whisper-base/

# ──────────────────────────────────────────────────────────────────
# Xcode / Swift build artefacts
# ──────────────────────────────────────────────────────────────────
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/
*.xcscmblueprint
*.xccheckout

# Swift Package Manager
.swiftpm/
.build/
Package.resolved

# ──────────────────────────────────────────────────────────────────
# macOS / editor noise
# ──────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
*.swp
*.swo
.idea/
*.xcuserdatad/

# ──────────────────────────────────────────────────────────────────
# Logs and produced artefacts
# ──────────────────────────────────────────────────────────────────
*.log
*.app
*.dSYM
*.dSYM.zip
*.ipa
*.xcarchive
Dictly-*.zip

# Compiled bytecode / packages we don't ship
*.class
*.jar
*.war
*.py[cod]
node_modules/
dist/
target/

# Large media files
*.mp4
*.tiff
*.avi
*.flv
*.mov
*.wmv
